<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="horizontal"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="center">

    <include
        layout="@layout/game_board"
        android:id="@+id/game_board"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"/>

    <TextView
        android:padding="@dimen/clickcount_padding_land"
        android:id="@+id/click_count"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="@dimen/textsize_clickcount_land"
        android:layout_marginBottom="@dimen/clickcount_margin_bottom"
        android:layout_toRightOf="@id/game_board"
        android:text="0"/>

    <Button
        android:id="@+id/play_again"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:visibility="gone"
        android:padding="@dimen/playagain_padding_land"
        android:layout_marginLeft="@dimen/playagain_margin_land"
        android:layout_below="@+id/click_count"
        android:layout_toRightOf="@+id/game_board"
        android:text="@string/play_again"/>

</RelativeLayout>